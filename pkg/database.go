package pkg

import "bankCLI/pkg/models"

var Percent = 10

var Cities = make([]models.City, 0)

var Clients = make([]models.Clients, 0)

var Bank = make([]models.Bank, 0)

var Transfers = make([]models.Transfers, 0)
